export namespace ValidateServiceInput {
  export type ValidateUser = {
    userId: string,
  };
  
  export type ValidateQuestion = {
    questionId: string,
  };

  export type ValidateAnswer = {
    answerId: string,
  };
}